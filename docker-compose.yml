version: "3.7"
services:
#  nginx:
#    image: nginx:latest
#    container_name: nginx
#    ports:
#      - "80:80"
#    volumes:
#      - ./config/nginx.conf:/etc/nginx/nginx.conf
#    restart: "unless-stopped"
  app:
    image: begin0dev/blog-server:1.0.0
    container_name: app
    restart: always
    ports:
      - "3001:3001"
